using System;

namespace Talkative.Tests
{
	public class TestNode
	{
		public TestNode ()
		{
			// Trigger triggers all actions
			// Trigger returns if no actions
			// TestConditions returns true if all fulfilled
			// TestConditions returns false if all don't fulfill
			// TestConditions returns false if one doesn't fulfill
			// TestConditions returns true if no conditions
		}
	}
}
